import {Box} from '@mui/material';
import React from 'react';

const PagesContent = () => {
  return (
    <Box
      sx={{
        gridArea: 'content',
        height: '100%',
      }}
    >
      PagesContent
    </Box>
  );
};

export default PagesContent;
